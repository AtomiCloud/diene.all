# Flutter variant

Declare the screen as a `go_router` route and keep route parsing outside the
render tree. Compose with `SafeAreaShell`, responsive slivers or scroll views,
Material semantics, and logical directional padding. Preserve scroll state on
back navigation and test all locales at a narrow phone width.
