# Flutter variant

Use `Form`/`FormField` with validation on change, stable keys, autofill hints,
and appropriate text input actions. Back drafts with `PersistentFormField` or
a typed repository. Use `AmountInput`, pickers, and bottom-sheet selectors for
non-free-text data. Expose explicit clear, restore, submit, and reset triggers.
