[test]
root = "tests/unit"
coverageReporter = ["text", "lcov"]
coverageDir = "coverage/unit"
coverageSkipTestFiles = true
# Unit ledger = every shipped utility module at 100%; the barrel and tests are glue.
coveragePathIgnorePatterns = ["src/index.ts", "tests/**"]
