// Package controllers holds the thin controller-runtime glue. No reconciler
// exists in the Phase-2 foundation window; this sentinel keeps the package
// compiling until the first real controller lands. The package carries no
// domain decisions, as enforced by the architecture gates.
package controllers
