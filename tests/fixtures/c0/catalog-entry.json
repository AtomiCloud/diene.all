{
  "$provenance": "C0 §14 problem-catalog schema: each problems[] entry is { id, type, title, status, recoverable, data, endpoints[] }. Authority: docs/standards/contracts/problem-catalog.md on the shared branch; mirrored at goals/c0-contracts.md §14. Hand-materialized 2026-07-22 at lib/go/errors-problems from the accepted lib/dart/problems fixture (identical cross-language artifact), re-validated against C0 §14. NOT a machine-generated C0 artifact.",
  "requiredFields": ["id", "type", "title", "status", "recoverable", "data", "endpoints"],
  "sample": {
    "id": "validation-error",
    "type": "https://docs.raichu.cluster.atomi.cloud/docs/raichu/dotnet/user/api/v1/validation-error",
    "title": "Validation error",
    "status": 400,
    "recoverable": true,
    "data": {
      "type": "object",
      "properties": {
        "fields": { "type": "array" }
      }
    },
    "endpoints": [{ "method": "POST", "path": "/user" }]
  }
}
