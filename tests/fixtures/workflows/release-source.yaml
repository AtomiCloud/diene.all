name: Release
on:
  workflow_run:
    workflows: [Build]
    branches: [main]
    types: [completed]
concurrency:
  group: release
jobs:
  release:
    if: github.event.workflow_run.conclusion == 'success'
    uses: ./.github/workflows/⚡reusable-release.yaml
