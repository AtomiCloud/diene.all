name: Release
on:
  workflow_run:
    workflows: [CI]
    branches: [main]
    types: [completed]
concurrency:
  group: parallel-release
jobs:
  release:
    if: github.event.workflow_run.conclusion == 'success'
    uses: ./.github/workflows/⚡reusable-release.yaml
