meta {
  name: 01 info
  seq: 2
}

docs {
  The dependency-blind info endpoint, the OpenAPI document, and the two system
  routes the ServerEngine ships. These are the routes both the liveness and the
  readiness probe target, so they must answer without touching any dependency.
}
