# ### flutter-base-reusable-build-ios
# #### source: flutter-base
name: ⚡ Reusable Build iOS

on:
  workflow_call:

jobs:
  build:
    name: Build IPA Donor
    runs-on:
      - nscloud-macos-sequoia-arm64-6x14-with-cache
      - nscloud-cache-size-50gb
      - nscloud-cache-tag-atomi-nix-store-cache-darwin-arm64
    timeout-minutes: 60
    env:
      APP_STORE_CONNECT_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_ISSUER_ID }}
      APP_STORE_CONNECT_KEY_IDENTIFIER: ${{ secrets.APP_STORE_CONNECT_KEY_IDENTIFIER }}
      APP_STORE_CONNECT_PRIVATE_KEY: ${{ secrets.APP_STORE_CONNECT_PRIVATE_KEY }}
      CERTIFICATE_PRIVATE_KEY: ${{ secrets.CERTIFICATE_PRIVATE_KEY }}
    steps:
      - uses: AtomiCloud/actions.setup-nix@v3
      - uses: namespacelabs/nscloud-cache-action@v1
        with:
          path: |
            ~/.pub-cache
            ~/.cocoapods
      - name: Select newest Xcode
        run: |
          set -euo pipefail
          xcode=$(printf '%s\n' /Applications/Xcode*.app | sort -V | tail -1)
          echo "DEVELOPER_DIR_OVERRIDE=$xcode/Contents/Developer" >> "$GITHUB_ENV"
      - name: Build donor IPA
        run: nix develop .#cd-ios -c ./scripts/ci/cd-ios.sh
      - uses: actions/upload-artifact@v4
        with:
          name: ipa
          path: build/ios/ipa/*.ipa
          if-no-files-found: error
          retention-days: 14
