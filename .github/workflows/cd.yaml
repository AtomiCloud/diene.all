# ### dart-result-cd
# #### source: lib/dart/result
name: CD

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  publish:
    name: Publish to pub.dev
    permissions:
      contents: read
    uses: ./.github/workflows/⚡reusable-publish.yaml
    secrets: inherit
