name: diene_core_utils
description: Pure Dart utilities for canonical keys, layered configuration, and C0 wire formats.
version: 0.1.0
repository: https://github.com/AtomiCloud/diene.dart_core-utils
issue_tracker: https://github.com/AtomiCloud/diene.dart_core-utils/issues

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  unorm_dart: ^0.3.2

dev_dependencies:
  coverage: ^1.15.0
  crypto: ^3.0.7
  lints: ^6.0.0
  test: ^1.26.0
