---
name: config
description: Follow the repository standard when changing the config loader, 4-tier merge, env-override contract, build-time variant, schema generation, or its TestHelper.
---

Read [docs/standards/config/index.md](../../../docs/standards/config/index.md)
before changing the config engine surface, the 4-tier merge, the env-override
contract, the `/build-time` variant, `$schema` generation, or the meta-test
contract.
