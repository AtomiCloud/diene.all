apiVersion: apps/v1
kind: Deployment
metadata:
  name: latest-image
spec:
  template:
    metadata:
      labels:
        app: demo
    spec:
      initContainers: []
      containers:
        - name: app
          image: nginx:latest
