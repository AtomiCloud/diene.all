apiVersion: apps/v1
kind: Deployment
metadata:
  name: latest-image
spec:
  selector:
    matchLabels:
      app: demo
  template:
    metadata:
      labels:
        app: demo
    spec:
      initContainers: []
      containers:
        - name: app
          image: nginx:latest
