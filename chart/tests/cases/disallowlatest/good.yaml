apiVersion: apps/v1
kind: Deployment
metadata:
  name: tagged-image
spec:
  selector:
    matchLabels:
      app: demo
  template:
    metadata:
      labels:
        app: demo
    spec:
      initContainers: []
      containers:
        - name: app
          image: nginx:1.27.2
