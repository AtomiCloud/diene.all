apiVersion: apps/v1
kind: Deployment
metadata:
  name: registry-port-no-tag
spec:
  template:
    spec:
      initContainers: []
      containers:
        - name: app
          image: registry.example.invalid:5000/team/app
