apiVersion: apps/v1
kind: Deployment
metadata:
  name: platform-ok
  labels:
    atomi.cloud/platform: nitroso
spec:
  template:
    metadata:
      labels:
        app: demo
    spec:
      containers:
        - name: app
          image: demo:1.0.0
