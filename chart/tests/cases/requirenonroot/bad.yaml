apiVersion: apps/v1
kind: Deployment
metadata:
  name: root-container
spec:
  selector:
    matchLabels:
      app: demo
  template:
    metadata:
      labels:
        app: demo
    spec:
      initContainers: []
      securityContext:
        runAsNonRoot: true
      containers:
        - name: app
          image: demo:1.0.0
          securityContext:
            runAsNonRoot: false
