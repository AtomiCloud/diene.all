apiVersion: v2
name: diene-zinc
description: Pure own-resources cert-manager issuer chart (Let's Encrypt ACME DNS-01 ClusterIssuers over Cloudflare) for AtomiCloud platform landscapes
type: application
version: 0.1.0
appVersion: '0.1.0'
kubeVersion: '>=1.22.0-0'
