# Independent local-cluster overlay. It contains no landscape decision.
# The cluster slot flows into the derived upstream.global.commonLabels via
# scripts/local/gen-identity-values.sh; no static label mirror is kept here.
serviceTree:
  cluster: lapras
