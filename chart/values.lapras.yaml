# Independent landscape overlay. Lapras uses k3s/k3d, which bundles
# metrics-server, so xenon is OFF and the whole wrapper renders nothing.
global:
  serviceTree:
    landscape: lapras

metricsServer:
  enabled: false
