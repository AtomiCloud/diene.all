/// The generated, version-pinned C0 §3 Config contract.
///
/// This façade exports the projection generated from the neutral
/// `c0-fixtures-r1` release. The generated implementation and its source
/// release are never hand-edited in this package.
library;

export 'src/c0_config_contract.g.dart';
