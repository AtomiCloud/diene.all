apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "fleet-operator.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "fleet-operator.labels" . | nindent 4 }}
