# ### dotnet-api-chart
# #### source: dotnet-api
apiVersion: v2
name: dotnet-api
description: Runtime app chart for the diene .NET API — Deployment, Service, HTTPRoute, HPA, ExternalSecret, the vendored settings ConfigMap, and the hook-scoped db-init Job
type: application
version: 0.0.0
appVersion: '0.0.0'
kubeVersion: '>=1.27.0-0'
