# Changelog

All notable changes are generated by the release pipeline.

## 0.0.0

- Initial `diene_auth_engine` surface: Logto provider seam, per-resource token
  cache (`IAuth`), session token lifecycle (10m access / 14d rotating refresh /
  re-mint on open), multi-backend claims-first onboarding phase machine,
  deferred-login mobile client (carrier + redeem), returnTo continuation,
  sign-up-only Doc B landscape selector, engine-owned `authEngine` config block
  schema, and the dependency-light TestHelper sub-library.
